function toFreud(str) {
  let alphabet = `abcdefghijklmnopqrstuvwxyz`.split``
  let thing = `${alphabet[18]}${alphabet[4]}${alphabet[23]}`
  
  return str[0] ? str.split` `.map(v=>thing).join` ` : ``
}
